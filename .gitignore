.venv
.idea
/backend/db/ask.db
/develop_data/
/.env
# legacy names kept ignored so stale copies on old checkouts never get committed
/.env.v2
/.env.v1.bak
/back_repo/
/hostlibs/
/logs/

# Python cache files
*.pyc
*.pyo
*.pyd
__pycache__/
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# backend/data — only askcfgs is tracked; everything else fills up during operation
/backend/data/*
!/backend/data/askcfgs/
!/backend/data/askcfgs/**
# The poll agent's build. Tracked on purpose, unlike every other artifact
# here: the offline server is fed by git bundles, so a build that is not in
# git never reaches it. A zip rather than the .exe — the archive saves nothing
# on size, but an .exe travelling to an operator's browser is what proxies and
# mail filters strike out. Only the current one is kept in the tree.
!/backend/data/agent/
!/backend/data/agent/hlv-poller-*.zip

# Shipped dictionary, not operational data: the accidents report reads it to
# turn DPD's event keys into wording we control.
!/backend/data/dpd_event_names.json

# Database dumps
/backend/data/server_dump.sql
/backend/data/server_dump.dump
/backend/data/*.backup
/backend/data/local_backup_*.sql
/backend/data/hostlib_backup

# Prediction output
/backend/data/*_output/
/backend/data/hourly_prediction_results.xlsx
*.parquet

# Pobut - not part of FastAPI server
/backend/pobut/

# Logs
*.log
/backend/*.log
/backend/*.txt
**/logs/
packages/
.claude/

# Build/dev artifacts that were committed by accident (now untracked)
*_results.txt
integration_test_output.log

# Test coverage output
.coverage
coverage.xml
htmlcov/

# Research outputs — research/ code is tracked, generated data is not
/research/*_output/
/research/*.xlsx
/research/*.csv
/research/*.png

# Preload JSON backups
backend/db/preload_db/*_old.json

# Local probe/debug scripts
/scripts/probe_hostlib/
